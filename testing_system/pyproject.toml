[project]
name = "testing-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.1.0",
    "fastapi[all]>=0.115.12",
    "httpx>=0.28.1",
    "pytest>=8.3.5",
]
